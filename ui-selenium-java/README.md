## Selenium UI Automation (Java)

This module demonstrates enterprise-style UI automation using Selenium and Java.

Key design decisions:
- Page Object Model with minimal abstraction
- Explicit waits only
- Parallel execution support
- Configuration via properties files

